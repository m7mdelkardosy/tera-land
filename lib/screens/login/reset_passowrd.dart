import 'package:flutter/material.dart';

class ResetPassowrd extends StatelessWidget {
  const ResetPassowrd({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("ResetPassowrd"),
      ),
    );
  }
}