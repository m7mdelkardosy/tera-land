import 'package:flutter/material.dart';

class MyLands extends StatelessWidget {
  const MyLands({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Center(
        child: Text("MyLands"),
      ),
    );
  }
}